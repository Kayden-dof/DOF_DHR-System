import pkg from '../package.json';

/* ---------------------------------------------------------------------------
   프로그램 판과 빌드

   §8.0 이 IQ 로 요구하는 것 하나가 "무엇이 깔려 있는가" 다. 종이에 적힌
   기록이 어느 판에서 나왔는지 되짚을 수 없으면 그 기록의 근거가 반쪽이다.

   판은 package.json 하나에서만 온다. 두 곳에 적으면 갈라진다 (§10).
--------------------------------------------------------------------------- */

export const APP_VERSION = pkg.version;

/** 배포된 빌드의 커밋. Vercel 이 넣어 준다. 로컬 실행에는 없다 */
export const BUILD_REF = process.env.VERCEL_GIT_COMMIT_SHA?.slice(0, 7) ?? null;
