node_modules/
.pgdata/
.pgdata-dev/
.next/
.env.local
.env.deploy
env.deploy
.env*.local
# 접속 정보가 든 파일은 이름을 하나씩 적지 않는다. 새 이름을 지을 때마다
# 여기 적는 것을 기억해야 하면 언젠가 잊는다 - 공개 저장소다 (2026-09-10)
.env.*
!.env.example
next-env.d.ts
*.tsbuildinfo

# 회사 CI 원본. 공개 저장소에 올리지 않는다. 필요한 벡터는 components/logo.tsx 에 있다
08. 회사 로고(CI).zip

# 사내 전용. 접속 정보와 운영 지식이 들어 있어 공개 저장소에 올리지 않는다
사내문서/

# 백업 산출물. 자료가 통째로 들어 있으므로 저장소에 올리지 않는다
backups/
.audit-tmp/

# 바깥에서 받은 스킬. 도구이지 이 제품이 아니다 (impeccable Apache-2.0 · taste-skill MIT)
.claude/skills/
