/* ---------------------------------------------------------------------------
   제품표준서 개정 사유 (사용자 요청 2026-09-01)

   개정 표기(rev.03)만 있고 왜 개정했는지가 어디에도 없었다. 나중에 개정 이력을
   훑어도 rev.01 · rev.02 · rev.03 이 나란히 있을 뿐, 무엇이 바뀌었는지 알려면
   서면 제품표준서를 꺼내 두 벌을 견줘야 한다.

   ── 한 줄이면 된다 ────────────────────────────────────────────────────────
   여기 적는 것은 **서면 제품표준서에 적힌 개정 사유를 옮긴 것**이다. 시스템이
   무엇이 바뀌었는지 계산하지 않는다 (§1). 판정도 아니고 승인도 아니다.

   ── 고쳐 쓸 수 있게 둔다 ──────────────────────────────────────────────────
   서명이 들어간 값이 아니고 판정도 아니므로 trg_once_written 을 걸지 않는다.
   오기를 고칠 길을 막으면 대장 밖의 종이에 적히게 된다. 고친 사실은 감사추적에
   남는다 (§5).

   ── 제품 코드도 만들 때 함께 받는다 ───────────────────────────────────────
   product_code · product_name 은 0031 에서 붙었는데 만든 뒤에 따로 넣게 되어
   있었다. 그래서 개정 추가 화면이 "대상 형명"(PD05050510)만 묻고, 정작 제품이
   무엇인지(DX2401)는 나중 일이 되었다. 열은 이미 있으므로 여기서는 화면만
   고치면 된다 - 이관에서 할 일은 없다.
--------------------------------------------------------------------------- */

alter table device_master add column if not exists note text;

comment on column device_master.note is
  '개정 사유. 서면 제품표준서에 적힌 것을 옮긴다. 시스템이 무엇이 바뀌었는지 계산하지 않는다';
