/* ---------------------------------------------------------------------------
   배치에 장입한 원재료를 재고에서 뺀다 (3차 검수 결함 3)

   material_lot.qty_available 을 움직이는 길은 둘뿐이다 - 자재 투입
   (material_issue) 과 재고 증감 (stock_movement). 그런데 원재료는 §5 에 따라
   자재 구성표에 넣지 않으므로 자재 투입이 생기지 않고, 작업 지시 발행도
   재고 증감을 만들지 않았다.

   그래서 배치에 넣은 원재료가 영원히 "사용 가능" 으로 남았다. 로컬 자료로
   확인한 것 - 여섯 배치가 120장을 장입했는데 차감 기록이 0건이고, 재고
   화면은 178장을 보여 주는데 실물은 58장이다.

   ── 어디에서 빼는가 ──────────────────────────────────────────────────────
   발행 시점이다. 배치당 원재료 로트가 하나로 확정되어 있고 (§4.5) 장입 장수도
   그때 정해진다. 그 뒤로는 나눌 일이 없다.

   응용이 아니라 트리거로 건다. 응용에 두면 작업 지시를 만드는 다른 길이
   생겼을 때 그 길만 재고를 안 빼고, 그런 어긋남은 한참 뒤에 실사에서 드러난다.

   ── 취소하면 되돌리는가 ──────────────────────────────────────────────────
   자동으로 되돌리지 않는다. 취소 시점에 그 원재료를 이미 뜯었는지 아닌지는
   시스템이 알 수 없고, 그건 판정이다 (§1). 되돌릴 것이 있으면 반납이나 조정
   기록으로 사유와 함께 남긴다.

   ── 원가는 건드리지 않는다 ───────────────────────────────────────────────
   원가는 unit_price × sheet_count 로 따로 세고 있고 그 값은 맞다. 어긋난 것은
   재고뿐이므로 재고만 고친다.
--------------------------------------------------------------------------- */

/* 배치 장입. 소모이므로 부호는 음수다 (§4.7 "반납 +, 폐기·소모 -") */
alter type movement_type add value if not exists 'BATCH_LOAD';
